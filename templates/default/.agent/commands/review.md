---
description: Review the current diff for bugs, then summarize risk
---
Review the current diff for bugs, correctness issues, and anything that
looks unfinished. Run `git diff` (and `git diff --staged` if needed) to see
what changed, focus on logic errors and edge cases over style, and end with
a short risk summary (low/medium/high) and why.

$ARGUMENTS
