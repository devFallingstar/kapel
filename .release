v0.9.0 release — image attachments work on every backend through @ mentions (bytes on native, -i paths on Codex, read-the-path on Claude Code); customizable handoff guidance via .agent/handoff.md; delegated worker usage attribution; kapel init seeds enabled validators from the repo's own scripts; compile-time warning for orchestrator-role routing targets; slash-only sessions listed; worktree-add race fixed
