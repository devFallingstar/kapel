v0.10.1 release — fresh projects set themselves up without asking: opening the REPL in a repo with no .agent/ runs init and the policy compile immediately with a one-line announcement; --no-setup and piped runs still opt out
