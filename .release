v0.10.0 release — multiple backends at once: multi-select setup with per-role backend+model pairs and mixed per-agent execution, per-directory config overrides, login checks with on-the-spot codex login and claude auth login plus /login, automatic project setup on first run, and a startup dashboard with /stats backed by persisted usage history
