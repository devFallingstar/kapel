v0.15.0 release — an edited policy keeps up with itself: a lock that no longer describes .agent/orchestration.md is now a state kapel handles rather than an error it reports, so editing a canonical policy in your editor costs nothing and needs no command — the next startup re-reads it — while a policy rewritten as prose is still deferred to the /plan or /orchestrate that asks for it
