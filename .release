v0.16.0 release — the REPL listens while it works: a line typed during a turn steers it (or queues for the next one), an orchestration run shows a live per-worker row, every turn ends with a change summary and /diff shows the whole thing, long waits ring the terminal bell, ! runs a shell command (!! carries its output into your next message), and Ctrl+R searches prompt history
