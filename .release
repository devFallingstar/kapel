v0.8.0 release — REPL-first breaking change: one-shot commands and flags removed (exec/plan/orchestrate/resume/worker, -i/-y/--json global/--system/--sandbox/--worker-mode/--dry-run/--max-iterations), /plan and /resume-run and /runs in the REPL, terse unknown-command errors, backend auto-detection when nothing is configured
